<%= form_with(model: mini_game, local: true) do |form| %>
  <% if mini_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mini_game.errors.count, "error") %> prohibited this mini_game from being saved:</h2>

      <ul>
      <% mini_game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
